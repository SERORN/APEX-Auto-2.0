import React, { useEffect } from 'react';

const Sidebar = () => {
  useEffect(() => {
    // Aquí se conectará con Supabase
  }, []);
  // TODO: Sidebar de navegación para el panel de proveedor
  return <div>{t('Sidebar')}</div>;
};

export default Sidebar;
