import React, { useEffect } from 'react';

const ProductTable = () => {
  useEffect(() => {
    // Aquí se conectará con Supabase
  }, []);
  // TODO: Tabla para mostrar productos del proveedor
  return <div>{t('Tabla de Productos')}</div>;
};

export default ProductTable;
