import React, { useEffect } from 'react';

const Header = () => {
  useEffect(() => {
    // Aquí se conectará con Supabase
  }, []);
  // TODO: Header del panel de proveedor
  return <div>{t('Header')}</div>;
};

export default Header;
