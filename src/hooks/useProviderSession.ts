// TODO: Hook para proteger rutas y verificar sesión del proveedor
// Debe validar si hay sesión activa y si el rol es "proveedor"
// Si no cumple, redirige al login
// Aquí se conectará con Supabase
export function useProviderSession() {
  // ...
}
