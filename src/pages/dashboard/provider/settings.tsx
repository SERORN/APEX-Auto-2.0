import React, { useEffect } from 'react';

const Settings = () => {
  useEffect(() => {
    // Aquí se conectará con Supabase
  }, []);
  // TODO: Configuración de cuenta del proveedor
  return <div>{t('Configuración de Cuenta')}</div>;
};

export default Settings;
