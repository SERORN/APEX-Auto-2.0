import React, { useEffect } from 'react';

const ProviderDashboardLayout = () => {
  useEffect(() => {
    // Aquí se conectará con Supabase
  }, []);
  // TODO: Layout general del panel de proveedor
  return <div>{t('Panel de Proveedor')}</div>;
};

export default ProviderDashboardLayout;
