import React, { useEffect } from 'react';

const UploadCSV = () => {
  useEffect(() => {
    // Aquí se conectará con Supabase
  }, []);
  // TODO: Subida masiva de productos por CSV
  return <div>{t('Subida masiva de productos')}</div>;
};

export default UploadCSV;
