import React, { useEffect } from 'react';

const Products = () => {
  useEffect(() => {
    // Aquí se conectará con Supabase
  }, []);
  // TODO: Gestión de productos del proveedor
  return <div>{t('Gestión de Productos')}</div>;
};

export default Products;
